{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsNestDomain",
  "title": "Nest Loader Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the loader.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What entity would you like to use for this loader (plural, e.g., \"users\")?"
    },
    "by": {
      "type": "string",
      "description": "The name of the loader by.",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What field would you like to use for this loader by?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the loader."
    },
    "sourceRoot": {
      "type": "string",
      "description": "Nest loader source root directory."
    },
    "flat": {
      "type": "boolean",
      "default": false,
      "description": "Flag to indicate if a directory is created."
    },
    "skipImport": {
      "type": "boolean",
      "description": "Flag to skip the module import.",
      "default": false
    }
  },
  "required": ["name", "by"]
}
